# Directorio de Datos

Este directorio contiene la base de datos SQLite del sistema.

## Archivos

- `streamhub.db` - Base de datos principal SQLite
- `streamhub.db-shm` y `streamhub.db-wal` - Archivos temporales de SQLite (modo WAL)

## Nota

Los archivos de la base de datos están en `.gitignore` para evitar conflictos entre diferentes entornos.
